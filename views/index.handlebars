<h1>flashcards</h1>

<h2>subjects that are not english!</h2>

<ul>
  {{#each flashcards}}
    {{#unless english}}
      {{> flashcards/flashcard-block english=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>subjects that are english!</h2>

<ul>
  {{#each flashcards}}
    {{#if english}}
      {{> flashcards/flashcard-block english=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a flashcard</h2>
<form class="create-form">

  <div class="form-group">
    <label for="question">Question:</label>
    <input type="text" id="question" name="name">
    <label for="answer">Answer:</label>
    <input type="text" id="answer" answer="answer">
  </div>

  <div class="form-group">
    <label for="english">English?</label><br>
    <input type="radio" name="english" value="1" checked> English<br>
    <input type="radio" name="english" value="0"> Not english
  </div>

  <button type="submit">Add flashcard</button>
</form>
