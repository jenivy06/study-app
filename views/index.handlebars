<h1>flashcards</h1>

<h2>subjects that are english!</h2>

<ul>
  {{#each flashcards}}
    {{#if english}}
      {{> flashcards/flashcard-block english=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>subjects that are history!</h2>

<ul>
  {{#each flashcards}}
    {{#if history}}
      {{> flashcards/flashcard-block history=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>subjects that are math!</h2>

<ul>
  {{#each flashcards}}
    {{#if math}}
      {{> flashcards/flashcard-block math=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a flashcard</h2>
<form class="create-form">

  <div class="form-group">
    <label for="question">Question:</label>
    <input type="text" id="question" name="name">
    <label for="answer">Answer:</label>
    <input type="text" id="answer" answer="answer">
  </div>

  <div class="form-group">
    <label for="english"></label><br>
    <input type="radio" name="english" value="1"> English<br>
  </div>
  <div class="form-group">
    <label for="history"></label><br>
    <input type="radio" name="history" value="1"> History<br>
  </div>
  <div class="form-group">
    <label for="math"></label><br>
    <input type="radio" name="math" value="1"> Math<br>
  </div>

  <button type="submit">Add flashcard</button>
</form>